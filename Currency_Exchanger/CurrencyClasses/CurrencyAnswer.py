class CurrencyAnswer():
    def __init__(self, currency_root, currency_targets, amount_root, amount_targets, exchange_rates):
        self.currency_root = currency_root
        self.currency_targets = currency_targets
        self.amount_root = amount_root
        self.amount_targets = amount_targets
        self.exchange_rates = exchange_rates